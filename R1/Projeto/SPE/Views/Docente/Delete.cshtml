@model SPE.ViewModel.DocenteViewModel

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>DocenteViewModel</legend>

    <div class="display-label">IdAgendaComponente</div>
    <div class="display-field">@Model.IdAgendaComponente</div>

    <div class="display-label">IdTipoContrato</div>
    <div class="display-field">@Model.IdTipoContrato</div>

    <div class="display-label">IdEmpresa</div>
    <div class="display-field">@Model.IdEmpresa</div>

    <div class="display-label">CPF</div>
    <div class="display-field">@Model.CPF</div>

    <div class="display-label">Email</div>
    <div class="display-field">@Model.Email</div>

    <div class="display-label">Sexo</div>
    <div class="display-field">@Model.Sexo</div>

    <div class="display-label">Tel</div>
    <div class="display-field">@Model.Tel</div>

    <div class="display-label">NivelFuncao</div>
    <div class="display-field">@Model.NivelFuncao</div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}


